[package]
name = "network"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
parking_lot = "0.12"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
consensus_core = { path = "../consensus/core" }
